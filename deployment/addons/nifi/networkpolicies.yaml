---
# Peers as in components/nifi, plus ingress-nginx for the UI.
nifi:
  podSelector:
    app.kubernetes.io/name: nifi
    app.kubernetes.io/instance: nifi-nifi
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: apisix
          componentNamespace: apisix
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: config-adapter
          componentNamespace: config-adapters
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nifi-bootstrap
          componentNamespace: nifi
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: nifi
              app.kubernetes.io/instance: nifi-nifi
          componentNamespace: nifi
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
