---
# Replaces components/nifi (apache-nifi-helm) with a Stackable NifiCluster.
component: nifi
parts:
  - name: nifi
    timeout: 600
  - name: bootstrap
    timeout: 900
    needs:
      - nifi.nifi
